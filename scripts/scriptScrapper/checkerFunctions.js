var model = require("./index");
var Kind = model.Kind; 
var Movie = model.Kind;



//string => 
function hasMultActresses  (title) {
}

//string => promise 
function getCharNames () {
	//returns CharNames
}

module.exports = {
	isString: isString,
	findKind: findKind
};